#include "main.h"

/**
 * _putnum - prints numbers
 * @n: number to be printed
 *
 * Return: 0 on success
 */

int _putnum(va_list input)
{
	char n;
	
	n = va_arg(input, int);
	for (n = '0'; n <= '9'; n++)
	{
		_putchar(n);
	}
	return (0);
}
