#ifndef HEADER_FILE
#define HEADER_FILE

#include <unistd.h>
int _putchar(char c);
void _putstring(char *s);
int _strlen(const char *s);
void _putnum(char n);
int _printf(const char *format, ...);

#endif
