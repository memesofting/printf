#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 *
 * Return: void on success
 */

int main(void)
{
	int len;
	char *s;

	s = "Let's do it";
	len =_strlen(s);
	_printf("ready\n");
	_printf("ready %c\n", 'b');
	_printf("%s\n", "mummy yo!");
	_printf("30%% of the deal\n");
	_printf("dd%gkk");
	_printf("Length: %d", len);
	return (0);
}
